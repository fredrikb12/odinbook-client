import styled from "styled-components";
import GenericButton from "../GenericButton";

export const StyledLoginButton = styled(GenericButton)`
  width: clamp(150px, 15vw, 300px);
  padding: 8px;
  border-radius: 6px;
  border: none;
`;
